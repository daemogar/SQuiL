// RELEASE_TAG is stamped at publish time by .github/workflows/publish.yml,
// which replaces the placeholder below with the GitHub release tag (e.g.
// "10.0.100.0042-beta"). Local/dev builds keep the placeholder; the update
// checker treats that as a dev build and skips the automatic check.
export const RELEASE_TAG = '__SQUIL_RELEASE_TAG__';
