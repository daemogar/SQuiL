//HintName: PositionalEnabledFalse_NoTransactionInjectedDataContext.g.cs
// <auto-generated />

#nullable enable

using Microsoft.Data.SqlClient;

using System;
using System.Collections.Generic;
using System.Data.Common;
using System.Threading;
using System.Threading.Tasks;

using SQuiL;

namespace TestCase;

partial class PositionalEnabledFalse_NoTransactionInjectedDataContext : SQuiLBaseDataContext
{
	public async Task<SQuiLResultType<PositionalEnabledFalse_NoTransactionInjectedResponse>> ProcessPositionalEnabledFalse_NoTransactionInjectedAsync(
		PositionalEnabledFalse_NoTransactionInjectedRequest request,
		CancellationToken cancellationToken = default!)
	{
		var builder = ConnectionStringBuilder("Db");
		using var connection = CreateConnection(builder.ConnectionString);
		
		var command = connection.CreateCommand();
		
		List<DbParameter> parameters = new()
		{
			CreateParameter("@Param_Id", System.Data.SqlDbType.BigInt, request.Id)
		};
		
		command.CommandText = Query(parameters);
		command.Parameters.AddRange(parameters.ToArray());
		
		await connection.OpenAsync(cancellationToken);
		
		PositionalEnabledFalse_NoTransactionInjectedResponse response = new();
		
		var isCount = false;
		
		List<SQuiLError> errors = [];
		try
		{
			using var reader = await command.ExecuteReaderAsync(cancellationToken);
			
			do
			{
				var __shape = ShapeKey(reader);
				switch (__shape)
				{
					case "count:int":
					{
						if (isCount) throw new Exception(
							"Already returned value for `Count`");
						
						isCount = true;
						
						if (!await reader.ReadAsync(cancellationToken)) break;
						
						response.Count = !reader.IsDBNull(0) ? reader.GetInt32(0) : throw new NullReferenceException("Return value for Count cannot be null.");
						break;
					}
				}
			}
			while (await reader.NextResultAsync(cancellationToken));
		}
		catch(SqlException e)
		{
			errors.Add(new(e.Number, 11, e.State, e.LineNumber, e.Procedure, e.Message));
		}
		
		if (!isCount) errors.Add(new(51001, 12, 1, 73, "Count", "Expected return scaler `Count`"));
		
		if(errors.Count == 0)
			return new(response);
		
		return new(errors);
		
		string Query(List<DbParameter> parameters) => $"""
		Declare @Return_Count int;
		
		Use [{builder.InitialCatalog}];
		
		Update [Documents] set Status = 'Done' where Id = @Param_Id;
		Set @Return_Count = @@RowCount;
		Select @Return_Count;
		""";
	}
}
