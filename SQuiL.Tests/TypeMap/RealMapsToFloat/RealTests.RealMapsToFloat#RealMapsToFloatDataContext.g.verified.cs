//HintName: RealMapsToFloatDataContext.g.cs
// <auto-generated />

#nullable enable

using Microsoft.Data.SqlClient;

using System;
using System.Collections.Generic;
using System.Data.Common;
using System.Threading;
using System.Threading.Tasks;

using SQuiL;

namespace TestCase;

partial class RealMapsToFloatDataContext : SQuiLBaseDataContext
{
	public async Task<SQuiLResultType<RealMapsToFloatResponse>> ProcessRealMapsToFloatAsync(
		RealMapsToFloatRequest request,
		CancellationToken cancellationToken = default!)
	{
		var builder = ConnectionStringBuilder("SQuiLDatabase");
		using var connection = CreateConnection(builder.ConnectionString);
		
		var command = connection.CreateCommand();
		
		List<DbParameter> parameters = new()
		{
			CreateParameter("@Param_Input", System.Data.SqlDbType.Real, request.Input),
			CreateParameter("@Param_Rate", System.Data.SqlDbType.Real, request.Rate)
		};
		
		command.CommandText = Query(parameters);
		command.Parameters.AddRange(parameters.ToArray());
		
		await connection.OpenAsync(cancellationToken);
		
		RealMapsToFloatResponse response = new();
		
		var isOutput = false;
		
		List<SQuiLError> errors = [];
		try
		{
			using var reader = await command.ExecuteReaderAsync(cancellationToken);
			
			do
			{
				var __shape = ShapeKey(reader);
				switch (__shape)
				{
					case "output:float":
					{
						if (isOutput) throw new Exception(
							"Already returned value for `Output`");
						
						isOutput = true;
						
						if (!await reader.ReadAsync(cancellationToken)) break;
						
						response.Output = !reader.IsDBNull(0) ? reader.GetFloat(0) : throw new NullReferenceException("Return value for Output cannot be null.");
						break;
					}
				}
			}
			while (await reader.NextResultAsync(cancellationToken));
		}
		catch(SqlException e)
		{
			errors.Add(new(e.Number, 11, e.State, e.LineNumber, e.Procedure, e.Message));
		}
		
		if (!isOutput) errors.Add(new(51001, 12, 1, 74, "Output", "Expected return scaler `Output`"));
		
		if(errors.Count == 0)
			return new(response);
		
		return new(errors);
		
		string Query(List<DbParameter> parameters) => $"""
		Declare @Return_Output real;
		
		Use [{builder.InitialCatalog}];
		
		Set @Return_Output = @Param_Input + @Param_Rate;
		""";
	}
}
