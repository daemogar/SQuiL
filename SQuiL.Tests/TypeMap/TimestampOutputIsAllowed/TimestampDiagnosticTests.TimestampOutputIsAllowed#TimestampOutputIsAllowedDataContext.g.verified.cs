//HintName: TimestampOutputIsAllowedDataContext.g.cs
// <auto-generated />

#nullable enable

using Microsoft.Data.SqlClient;

using System;
using System.Collections.Generic;
using System.Data.Common;
using System.Threading;
using System.Threading.Tasks;

using SQuiL;

namespace TestCase;

partial class TimestampOutputIsAllowedDataContext : SQuiLBaseDataContext
{
	public async Task<SQuiLResultType<TimestampOutputIsAllowedResponse>> ProcessTimestampOutputIsAllowedAsync(
		TimestampOutputIsAllowedRequest request,
		CancellationToken cancellationToken = default!)
	{
		var builder = ConnectionStringBuilder("SQuiLDatabase");
		using var connection = CreateConnection(builder.ConnectionString);
		
		var command = connection.CreateCommand();
		
		List<DbParameter> parameters = new()
		{
		};
		
		command.CommandText = Query(parameters);
		command.Parameters.AddRange(parameters.ToArray());
		
		await connection.OpenAsync(cancellationToken);
		
		TimestampOutputIsAllowedResponse response = new();
		
		var isVersion = false;
		
		List<SQuiLError> errors = [];
		try
		{
			using var reader = await command.ExecuteReaderAsync(cancellationToken);
			
			do
			{
				var __shape = ShapeKey(reader);
				switch (__shape)
				{
					case "version:byte[]":
					{
						if (isVersion) throw new Exception(
							"Already returned value for `Version`");
						
						isVersion = true;
						
						if (!await reader.ReadAsync(cancellationToken)) break;
						
						response.Version = !reader.IsDBNull(0) ? reader.GetFieldValue<byte[]>(0) : throw new NullReferenceException("Return value for Version cannot be null.");
						break;
					}
				}
			}
			while (await reader.NextResultAsync(cancellationToken));
		}
		catch(SqlException e)
		{
			errors.Add(new(e.Number, 11, e.State, e.LineNumber, e.Procedure, e.Message));
		}
		
		if (!isVersion) errors.Add(new(51001, 12, 1, 72, "Version", "Expected return scaler `Version`"));
		
		if(errors.Count == 0)
			return new(response);
		
		return new(errors);
		
		string Query(List<DbParameter> parameters) => $"""
		Declare @Return_Version timestamp;
		
		Use [{builder.InitialCatalog}];
		
		Select @Return_Version;
		""";
	}
}
