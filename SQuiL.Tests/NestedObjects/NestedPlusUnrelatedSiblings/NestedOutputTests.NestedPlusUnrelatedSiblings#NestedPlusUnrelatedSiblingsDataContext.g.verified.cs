//HintName: NestedPlusUnrelatedSiblingsDataContext.g.cs
// <auto-generated />

#nullable enable

using Microsoft.Data.SqlClient;

using System;
using System.Collections.Generic;
using System.Data.Common;
using System.Threading;
using System.Threading.Tasks;
using System.Linq;

using SQuiL;

namespace TestCase;

partial class NestedPlusUnrelatedSiblingsDataContext : SQuiLBaseDataContext
{
	public async Task<SQuiLResultType<NestedPlusUnrelatedSiblingsResponse>> ProcessNestedPlusUnrelatedSiblingsAsync(
		NestedPlusUnrelatedSiblingsRequest request,
		CancellationToken cancellationToken = default!)
	{
		var builder = ConnectionStringBuilder("SQuiLDatabase");
		using var connection = CreateConnection(builder.ConnectionString);
		
		var command = connection.CreateCommand();
		
		List<DbParameter> parameters = new()
		{
		};
		
		command.CommandText = Query(parameters);
		command.Parameters.AddRange(parameters.ToArray());
		
		await connection.OpenAsync(cancellationToken);
		
		NestedPlusUnrelatedSiblingsResponse response = new();
		
		var isTotal = false;
		var isTranscript = false;
		var isInstitution = false;
		var isCourse = false;
		var isLog = false;
		
		List<SQuiLError> errors = [];
		try
		{
			List<TestCase.Models.Transcript> __Transcript = [];
			List<TestCase.Models.Institution> __Institution = [];
			List<TestCase.Models.Course> __Course = [];
			List<TestCase.Models.Log> __Log = [];
			
			using var reader = await command.ExecuteReaderAsync(cancellationToken);
			
			do
			{
				var __shape = ShapeKey(reader);
				switch (__shape)
				{
					case "transcriptid:int|issuedate:System.DateOnly":
					{
						isTranscript = true;
						
						if (!await reader.ReadAsync(cancellationToken)) break;
						
						var indexTranscriptID = reader.GetOrdinal("TranscriptID");
						var indexIssueDate = reader.GetOrdinal("IssueDate");
						
						do
						{
							var valueTranscriptID = reader.GetInt32(indexTranscriptID);
							var valueIssueDate = reader.GetFieldValue<System.DateOnly>(indexIssueDate);
							
							__Transcript.Add(new(
								valueTranscriptID,
								valueIssueDate));
						}
						while (await reader.ReadAsync(cancellationToken));
						break;
					}
					case "institutionid:int|transcriptid:int|schoolname:string":
					{
						isInstitution = true;
						
						if (!await reader.ReadAsync(cancellationToken)) break;
						
						var indexInstitutionID = reader.GetOrdinal("InstitutionID");
						var indexTranscriptID = reader.GetOrdinal("TranscriptID");
						var indexSchoolName = reader.GetOrdinal("SchoolName");
						
						do
						{
							var valueInstitutionID = reader.GetInt32(indexInstitutionID);
							var valueTranscriptID = reader.GetInt32(indexTranscriptID);
							var valueSchoolName = reader.GetString(indexSchoolName);
							
							__Institution.Add(new(
								valueInstitutionID,
								valueTranscriptID,
								valueSchoolName));
						}
						while (await reader.ReadAsync(cancellationToken));
						break;
					}
					case "courseid:int|institutionid:int|title:string":
					{
						isCourse = true;
						
						if (!await reader.ReadAsync(cancellationToken)) break;
						
						var indexCourseID = reader.GetOrdinal("CourseID");
						var indexInstitutionID = reader.GetOrdinal("InstitutionID");
						var indexTitle = reader.GetOrdinal("Title");
						
						do
						{
							var valueCourseID = reader.GetInt32(indexCourseID);
							var valueInstitutionID = reader.GetInt32(indexInstitutionID);
							var valueTitle = reader.GetString(indexTitle);
							
							__Course.Add(new(
								valueCourseID,
								valueInstitutionID,
								valueTitle));
						}
						while (await reader.ReadAsync(cancellationToken));
						break;
					}
					case "total:int":
					{
						if (isTotal) throw new Exception(
							"Already returned value for `Total`");
						
						isTotal = true;
						
						response.Total = !reader.IsDBNull(0) ? reader.GetInt32(0) : throw new NullReferenceException("Return value for Total cannot be null.");
						break;
					}
					case "logid:int|message:string":
					{
						isLog = true;
						
						if (!await reader.ReadAsync(cancellationToken)) break;
						
						var indexLogID = reader.GetOrdinal("LogID");
						var indexMessage = reader.GetOrdinal("Message");
						
						do
						{
							var valueLogID = reader.GetInt32(indexLogID);
							var valueMessage = reader.GetString(indexMessage);
							
							__Log.Add(new(
								valueLogID,
								valueMessage));
						}
						while (await reader.ReadAsync(cancellationToken));
						break;
					}
				}
			}
			while (await reader.NextResultAsync(cancellationToken));
			
			foreach (var parent in __Institution)
			{
				parent.Course = __Course.Where(c => c.InstitutionID == parent.InstitutionID).ToList();
			}
			foreach (var parent in __Transcript)
			{
				parent.Institution = __Institution.Where(c => c.TranscriptID == parent.TranscriptID).ToList();
			}
			
			response.Transcript = System.Linq.Enumerable.SingleOrDefault(__Transcript);
			response.Log = __Log;
		}
		catch(SqlException e)
		{
			errors.Add(new(e.Number, 11, e.State, e.LineNumber, e.Procedure, e.Message));
		}
		
		if (!isTotal) errors.Add(new(51001, 12, 1, 182, "Total", "Expected return scaler `Total`"));
		if (!isTranscript) errors.Add(new(51001, 12, 1, 183, "Transcript", "Expected return object `Transcript`"));
		if (!isInstitution) errors.Add(new(51001, 12, 1, 184, "Institution", "Expected return table `Institution`"));
		if (!isCourse) errors.Add(new(51001, 12, 1, 185, "Course", "Expected return table `Course`"));
		if (!isLog) errors.Add(new(51001, 12, 1, 186, "Log", "Expected return table `Log`"));
		
		if(errors.Count == 0)
			return new(response);
		
		return new(errors);
		
		string Query(List<DbParameter> parameters) => $"""
		Declare @Return_Transcript table(
			[TranscriptID] int,
			[IssueDate] date);
		
		Declare @Returns_Institution table(
			[InstitutionID] int,
			[TranscriptID] int,
			[SchoolName] varchar(50));
		
		Declare @Returns_Course table(
			[CourseID] int,
			[InstitutionID] int,
			[Title] varchar(50));
		
		Declare @Return_Total int;
		
		Declare @Returns_Log table(
			[LogID] int,
			[Message] varchar(50));
		
		Use [{builder.InitialCatalog}];
		
		Insert Into @Return_Transcript Select TranscriptID, IssueDate From T;
		Insert Into @Returns_Institution Select InstitutionID, TranscriptID, SchoolName From I;
		Insert Into @Returns_Course Select CourseID, InstitutionID, Title From C;
		Set @Return_Total = (Select Count(*) From T);
		Insert Into @Returns_Log Select LogID, Message From L;
		Select * From @Return_Transcript;
		Select * From @Returns_Institution;
		Select * From @Returns_Course;
		Select @Return_Total As Total;
		Select * From @Returns_Log;
		""";
	}
}
