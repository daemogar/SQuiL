//HintName: LengthOnlyDifferenceStillCollidesSP0030DataContext.g.cs
// <auto-generated />

#nullable enable

using Microsoft.Data.SqlClient;

using System;
using System.Collections.Generic;
using System.Data.Common;
using System.Threading;
using System.Threading.Tasks;

using SQuiL;

namespace TestCase;

partial class LengthOnlyDifferenceStillCollidesSP0030DataContext : SQuiLBaseDataContext
{
	public async Task<SQuiLResultType<LengthOnlyDifferenceStillCollidesSP0030Response>> ProcessLengthOnlyDifferenceStillCollidesSP0030Async(
		LengthOnlyDifferenceStillCollidesSP0030Request request,
		CancellationToken cancellationToken = default!)
	{
		var builder = ConnectionStringBuilder("SQuiLDatabase");
		using var connection = CreateConnection(builder.ConnectionString);
		
		var command = connection.CreateCommand();
		
		List<DbParameter> parameters = new()
		{
		};
		
		command.CommandText = Query(parameters);
		command.Parameters.AddRange(parameters.ToArray());
		
		await connection.OpenAsync(cancellationToken);
		
		LengthOnlyDifferenceStillCollidesSP0030Response response = new();
		
		var isA = false;
		var isB = false;
		
		List<SQuiLError> errors = [];
		try
		{
			using var reader = await command.ExecuteReaderAsync(cancellationToken);
			
			do
			{
				var __shape = ShapeKey(reader);
				switch (__shape)
				{
					case "note:string":
					{
						isA = true;
						
						response.A ??= [];
						if (!await reader.ReadAsync(cancellationToken)) break;
						
						var indexNote = reader.GetOrdinal("Note");
						
						do
						{
							var valueNote = reader.GetString(indexNote);
							
							response.A.Add(new(
								valueNote));
						}
						while (await reader.ReadAsync(cancellationToken));
						break;
					}
					case "note:string":
					{
						isB = true;
						
						response.B ??= [];
						if (!await reader.ReadAsync(cancellationToken)) break;
						
						var indexNote = reader.GetOrdinal("Note");
						
						do
						{
							var valueNote = reader.GetString(indexNote);
							
							response.B.Add(new(
								valueNote));
						}
						while (await reader.ReadAsync(cancellationToken));
						break;
					}
				}
			}
			while (await reader.NextResultAsync(cancellationToken));
		}
		catch(SqlException e)
		{
			errors.Add(new(e.Number, 11, e.State, e.LineNumber, e.Procedure, e.Message));
		}
		
		if (!isA) errors.Add(new(51001, 12, 1, 99, "A", "Expected return table `A`"));
		if (!isB) errors.Add(new(51001, 12, 1, 100, "B", "Expected return table `B`"));
		
		if(errors.Count == 0)
			return new(response);
		
		return new(errors);
		
		string Query(List<DbParameter> parameters) => $"""
		Declare @Returns_A table(
			[Note] varchar(50));
		
		Declare @Returns_B table(
			[Note] varchar(100));
		
		Use [{builder.InitialCatalog}];
		
		Select * From @Returns_A;
		Select * From @Returns_B;
		""";
	}
}
