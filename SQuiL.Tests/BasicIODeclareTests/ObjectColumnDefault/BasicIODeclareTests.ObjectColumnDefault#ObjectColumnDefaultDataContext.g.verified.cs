//HintName: ObjectColumnDefaultDataContext.g.cs
// <auto-generated />

#nullable enable

using Microsoft.Data.SqlClient;

using System;
using System.Collections.Generic;
using System.Data.Common;
using System.Threading;
using System.Threading.Tasks;

using SQuiL;

namespace TestCase;

partial class ObjectColumnDefaultDataContext : SqlServerDataContext
{
	public async Task<SQuiLResultType<ObjectColumnDefaultResponse>> ProcessObjectColumnDefaultAsync(
		ObjectColumnDefaultRequest request,
		CancellationToken cancellationToken = default!)
	{
		var builder = ConnectionStringBuilder("SQuiLDatabase");
		using var connection = CreateConnection(builder.ConnectionString);
		
		var command = connection.CreateCommand();
		
		List<DbParameter> parameters = new()
		{
		};
		
		command.CommandText = Query(parameters);
		command.Parameters.AddRange(parameters.ToArray());
		
		await connection.OpenAsync(cancellationToken);
		
		ObjectColumnDefaultResponse response = new();
		
		var isPerson = false;
		
		List<SQuiLError> errors = [];
		try
		{
			using var reader = await command.ExecuteReaderAsync(cancellationToken);
			
			do
			{
				var __shape = ShapeKey(reader);
				switch (__shape)
				{
					case "personid:int|note:string|score:int":
					{
						if (isPerson) throw new Exception(
							"Already returned value for `Person`");
						
						isPerson = true;
						
						if (!await reader.ReadAsync(cancellationToken)) break;
						
						if (response.Person is not null)
							throw new Exception("Person was already set.");
						
						var indexPersonID = reader.GetOrdinal("PersonID");
						var indexNote = reader.GetOrdinal("Note");
						var indexScore = reader.GetOrdinal("Score");
						
						response.Person = new(
							reader.GetInt32(indexPersonID),
							reader.IsDBNull(indexScore) ? default(int?) : reader.GetInt32(indexScore))
						{
							Note = reader.GetString(indexNote),
						};
						
						if (await reader.ReadAsync(cancellationToken))
							throw new Exception(
								"Return object results in more than one object. Consider using a return table instead.");
						
						break;
					}
				}
			}
			while (await reader.NextResultAsync(cancellationToken));
		}
		catch(SqlException e)
		{
			errors.Add(CreateError(e));
		}
		
		if (!isPerson) errors.Add(new(51001, 12, 1, 89, "Person", "Expected return object `Person`"));
		
		if(errors.Count == 0)
			return new(response);
		
		return new(errors);
		
		string Query(List<DbParameter> parameters) => $"""
		Declare @Return_Person table(
			[PersonID] int,
			[Note] varchar(50),
			[Score] int Null);
		
		Use [{builder.InitialCatalog}];
		
		Select PersonID, Note, Score From dbo.People;
		""";
	}
}
